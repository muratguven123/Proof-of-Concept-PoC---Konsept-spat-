_format_version: "3.0"

services:
  # Keycloak - Token almak icin proxy
  - name: keycloak-service
    url: http://keycloak:8080
    routes:
      - name: keycloak-route
        paths:
          - /auth
        strip_path: true
        preserve_host: true
